# Deployment records

`scripts/deploy.ts` writes a network-specific JSON file here and updates the
submission-level `addresses.txt`. Generated JSON deployment records are ignored
to avoid committing environment-specific runtime data.
